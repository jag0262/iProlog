//extends engine, implements spliterator

var Prog = class Prog {
  function Prog(fname) {
    //super(fname);
  }

  //input of type object
  function pp(o) {
    Main.pp(o);
  }
  //input of type object
  function println(o) {
    Main.println(o);
  }

  
  function showTerm(O) {
    //returns string
    
  }

  function maybeNull(O) {
    //returns string
    
  }

  function isListCons(name) {
    //returns bool
    //return ".".equals(name) || "[|]".equals(name) || "list".equals(name);
  }

  function isOp(name) {
    //returns bool
    //return "/".equals(name) || "-".equals(name) || "+".equals(name) || "=".equals(name);
  }
  //input is object array
  function st0(args) {
    //returns string
  }

  function ppCode() {
    //void function
  }
  //input type Clause
  function showClause(s) {
    //return string
   
  }

  

  //@Override
  //input type IntList
  function ppGoals(bs) {
    //void return type

  }

  //@Override
  //input type Spine
  vunction ppc(S) {
    /void return type
  }

  /////////////// end of show

  // possibly finite Stream support


}
