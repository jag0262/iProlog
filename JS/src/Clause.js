

var Clause = class Clause {
  function Clause(len, hgs, base, neck, xs){
    this.len = len;
    this.hgs = hgs;
    this.base = base;
    this.neck = neck;
    this.xs = xs;
  }
  
  var len = 0;
  var hgs = [];
  var base = 0;
  var neck = 0;
  var xs = [];
