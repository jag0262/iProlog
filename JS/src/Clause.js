class Clause {
    constructor(len, hgs, base, neck, xs){
      this.len = len;
      this.hgs = hgs;
      this.base = base;
      this.neck = neck;
      this.xs = xs;
    }
    
}

exports.main = function(){
  return true; 
}